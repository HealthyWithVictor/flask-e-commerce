# 基础框架
Flask>=3.0.0
# Flask 内部依赖，通常无需显式安装，但写上确保兼容性
Werkzeug>=3.0.0
Jinja2>=3.1.0
itsdangerous>=2.1.0

# 生产环境/部署相关的增强工具
# WhiteNoise 用于高效地服务静态文件
WhiteNoise>=6.6.0
# Flask-Talisman 用于增强安全性（HTTPS/CSP/HSTS）
Flask-Talisman>=0.8.1