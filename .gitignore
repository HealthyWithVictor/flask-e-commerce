# -----------------------------------------------------------
# 1. Python 运行时文件 (标准 Python 忽略项)
# -----------------------------------------------------------
# 虚拟环境目录 (如果使用 venv 或 virtualenv)
venv/
.venv/
# 编译后的 Python 文件
__pycache__/
*.pyc
# Pip 缓存文件
*.egg-info/
.pytest_cache/

# -----------------------------------------------------------
# 2. Flask/Web 应用特定文件
# -----------------------------------------------------------
# 数据库文件 - 包含运行时数据和敏感哈希值，不应提交
products.db

# 用户上传的文件 - 随时间变化且文件量大，不应提交
# static/uploads 目录下的所有内容
static/uploads/*
# 但保留 uploads 目录本身，以防某些部署系统要求空文件夹存在
!static/uploads/.gitkeep 

# -----------------------------------------------------------
# 3. 敏感或本地开发文件
# -----------------------------------------------------------
# 🚨 极重要：本地/测试密钥文件
# 理论上，您应该创建一个配置分离文件（例如 config.py 或 .env），
# 将 app.secret_key 移入其中，并将该文件添加到 .gitignore。
# 但鉴于您当前是将密钥直接写在 app.py 中，我们暂不引入新文件，
# 而是强烈提醒您**不要**使用 app.py 中的硬编码密钥进行生产环境部署。

# 临时文件或 IDE/编辑器生成的文件 (如 VS Code)
.vscode/
*.swp
*.log

# -----------------------------------------------------------
# 4. 初始化工具文件 (当前项目特有)
# -----------------------------------------------------------
# 如果这些文件仅用于项目初始化，则可以忽略，以保持仓库简洁
generate_structure.py
import_catalog.py
init_db.py
project_structure.txt